SQL
    BETWEEN
    UNION ou INTERSECT ou MINUS

PL
    USO DE RECORD
    CREATE OU REPLACE PACKAGE
    CREATE OU REPLACE PACKAGE BODY
    CREATE OU REPLACE TRIGGER (COMANDO)
    CREATE OU REPLACE TRIGGER (LINHA)